# Cursor／AI 協作備忘（RB-Stock-Analysis_V3）

> 來源：整合 `OneDrive/桌面/Cursor.md.md`（ChatGPT 草稿）與本 repo 實際狀況。  
> 已處理：移除逐行 `######`、修正檔名／跳脫字元、調整與現況不符的優先級敘述；草稿結尾未完成處改為可讀模板。

---

## 角色與權責

- 你是 **Cursor 內的實作與除錯角色**：寫程式、改檔、修 bug、依規格完成任務。
- **使用者在對話中的指示一律優先**；不要自行改變專案方向或產品定位。
- 若有顧問／規格檔（例如 `StockOC` 下的 `*_spec.md`），與使用者指示不衝突時一併遵循。
- （選用）若團隊另有「顧問模型／審查者」流程，僅在**使用者明確交辦**時扮演承接實作的一方；**不要**假設永遠由某一特定模型發號施令。

---

## 專案背景（與現況對齊）

- 專案名稱：**`RB-Stock-Analysis_V3`**（台股籌碼／主力／法人與資料管線）。
- 技術棧包含：**Python pipeline**（`scraper_chip.py`、`core/`）、**FinMind／TWSE** 資料、輸出至 **`data/`**；**靜態儀表板**（根目錄 **`index.html`**，GitHub Pages）。
- 目標涵蓋（依既有設計延續即可，不必重寫）：籌碼與分點、法人、異常資金流監控意涵、策略／回測支援、儀表板監看等。

---

## 實作原則（精簡）

1. **未經要求不要整包重寫**專案或大幅重構。
2. **較大改動**前先簡述計畫與將動到的檔案（小修小補可直接做）。
3. 偏好 **小步、可驗證** 的變更；先找根因再修 bug。
4. 新增依賴時更新 **`requirements.txt`**，並簡短說明用途。
5. **禁止**硬編 API key／token；使用 **環境變數** 或 **`.env`**（且 **勿 commit `.env`**）。
6. 程式以 **可讀、直白** 為主；避免過度抽象與不必要框架。
7. **未經明確要求**：不實作自動下單、不接 **實盤券商 API**、不做 live 下單邏輯；**研究／訊號／回測**可為。

---

## 開發優先級（校訂後）

草稿寫「在資料管線穩定前不要優先做 Dashboard UI」——**與本專案現況不符**（已有成熟靜態 Dashboard 與多 Phase 功能）。

- **預設**：在 **維持本機可跑、資料可更新** 的前提下，**管線與儀表板可並行**做小步迭代。
- **不要**未經討論就整體重排架構或目錄；新功能盡量沿用 `core/`、`sub-py/`、`scripts/` 慣例。

---

## 程式風格與模組

- 主語言 **Python**；清楚函式／變數命名；僅在有助理解時加註解。
- 設定與機密與業務邏輯分離；**抓資料** 與 **分析** 與 **前端呈現** 盡量分界（本 repo 已大致如此，延續即可）。
- 新模組命名可具語意（如 `data_loader`、`chip_analysis` 等），**但不必**為了「理想目錄」大搬風；穩定優先。

---

## 資料與 FinMind

- 原始與加工資料分離；轉換可重跑；重要欄位先驗證再分析。
- 假設 API 可能失敗、缺欄、缺日；錯誤要可見，避免默默吞掉。
- FinMind token：`FINMIND_API_TOKEN`（見 `.env`）；API 邏輯與分析分層。

---

## 儀表板（`index.html`）

- 核心分析邏輯應留在 **Python／JSON**；前端以呈現與互動為主（與現有 Phase 設計一致）。
- 優先 **清楚** 於炫目視覺；確保資料可從 `data/` 獨立檢視。

---

## 與本 repo 一致的實務備忘

- **回覆使用者**：**繁體中文**。
- **Git**：未經使用者明確要求 **不要** `commit`；若使用者要求 **commit／push**，遵守其既有規則（勿改 `git config`、勿對 main **force push**、勿擅自 `--no-verify` 等）。
- **路徑**：專案常在 **雲端同步碟**；I/O 慢、`venv` **勿從別台複製**；重建後用 **`.\venv\Scripts\python.exe -m pip ...`**。
- **一鍵**：`5_run_all_for_stock.bat`（CMD 中 `echo`／`set /p` 的括號需 **`^(` `^)`** 轉義，檔內已處理）。
- **爬蟲**：`scraper_chip.py`（需 FinMind token）；大盤情境：`scripts/generate_market_context.py`。
- **Telegram 日報**：`send_daily_report.py`（`TELEGRAM_BOT_TOKEN`、`TELEGRAM_CHAT_ID`）。
- **版號**：`Version.txt`（與儀表板徽章一致）。

---

## 變更前自述模板（草稿補完）

較大變更時可簡寫：

```text
Planned change:（要做什麼）
Files to touch:（檔案清單）
Reason:（為何）
Risk:（低／中／高；可能影響）
```

---

若你之後更新 OneDrive 上的草稿，可再貼上或覆寫本檔對應段落，由 AI 協助合併與去重。
